\ProvidesPackage{style/packages}

% font
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{kpfonts}
\usepackage[scaled=0.8]{sourcecodepro}

%bibliography
\usepackage{url}
\urlstyle{same}
%\usepackage[style=verbose-ibid, backend=biber]{biblatex}
%\addbibresource{references.bib}

% figures
\usepackage{graphicx}
\usepackage{float}
\usepackage{tikz}
\usetikzlibrary{positioning}

% estout
\usepackage{booktabs}
\usepackage{dcolumn}

% captions
\usepackage{caption}
\captionsetup{
    labelfont=bf,
    font=small,
    labelsep=colon
}

% headers
\usepackage{scrlayer-scrpage}
\clearpairofpagestyles

% import
\usepackage{import}

% minted
\usepackage{minted}
\setminted{style=stata-light,bgcolor=gray!5,linenos=true}

% tikz graphs
\usepackage{tikz}
\usetikzlibrary{shapes,arrows,positioning}

\tikzstyle{observed} = [rectangle, draw, thick, minimum width=2cm, minimum height=0.8cm, align=center]
\tikzstyle{unobserved} = [ellipse, draw, thick, dashed, minimum width=2cm, minimum height=0.8cm, align=center]
\tikzstyle{outcome} = [rectangle, draw, thick, fill=gray!20, minimum width=2cm, minimum height=0.8cm, align=center]
\tikzstyle{arrow} = [thick,->,>=stealth]